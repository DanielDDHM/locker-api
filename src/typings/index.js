const { STATUS } = require('./status')
const { QUERY } = require('./query')
const { REGEX } = require('./regex')
const { SIZES } = require('./sizes')
const { OPERATIONS } = require('./operations')

module.exports = { SIZES, REGEX, QUERY, STATUS, OPERATIONS }